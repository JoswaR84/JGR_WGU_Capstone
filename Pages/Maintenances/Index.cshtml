@page
@model JGR_WGU_Capstone.Pages.Maintenances.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Maintenance[0].Notes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Maintenance[0].MaintenanceDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Maintenance[0].Computer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Maintenance) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Notes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaintenanceDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Computer.Manufacturer)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MaintenanceID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MaintenanceID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MaintenanceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
